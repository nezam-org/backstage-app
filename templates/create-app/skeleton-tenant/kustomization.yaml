# templates/tenant/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - rbac.yaml
  - quota.yaml
  - netpol.yaml
  - database.yaml
  - sync.yaml
  - preview.yaml
  # 037: written by the nezam:tenant:db-provision scaffolder step (encrypted;
  # decrypted by the platform's `tenants` Flux Kustomization, ADR-027).
  - secrets.sops.yaml
